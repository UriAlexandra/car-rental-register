export enum VehicleType {
    CAR = 'CAR',
    WATERCRAFT = 'WATERCRAFT'
}

export enum VehicleStatus {
    FREE = 'FREE',
    RENTED = 'RENTED',
    SCRAPPED = 'SCRAPPED'
}

export enum RentalItemStatus {
    PENDING = 'PENDING',
    RETURNED = 'RETURNED',
    DAMAGED = 'DAMAGED',
    ACTIVE = 'ACTIVE'
}